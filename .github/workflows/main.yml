name: Global Stock Matrix Monitor

on:
  workflow_dispatch:
    inputs:
      market:
        description: '選擇執行市場 (all / tw / us / cn / hk / jp / kr)'
        required: true
        default: 'all'
        type: choice
        options: [all, tw, us, cn, hk, jp, kr]
  schedule:
    - cron: '30 10 * * 1-5' # 台北時間 18:30 執行

jobs:
  prepare-matrix:
    runs-on: ubuntu-latest
    outputs:
      matrix_list: ${{ steps.set-matrix.outputs.matrix_list }}
    steps:
      - id: set-matrix
        run: |
          if [[ "${{ github.event.inputs.market }}" == "all" || "${{ github.event_name }}" == "schedule" ]]; then
            echo "matrix_list=[\"tw\", \"us\", \"cn\", \"hk\", \"jp\", \"kr\"]" >> $GITHUB_OUTPUT
          else
            echo "matrix_list=[\"${{ github.event.inputs.market }}\"]" >> $GITHUB_OUTPUT
          fi

  execute-task:
    needs: prepare-matrix
    runs-on: ubuntu-latest
    # 💡 增加超時保護，防止 yfinance 卡死浪費分鐘數
    timeout-minutes: 180 
    strategy:
      fail-fast: false
      matrix:
        market: ${{ fromJson(needs.prepare-matrix.outputs.matrix_list) }}
    
    env:
      TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
      TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
      RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}
      GDRIVE_SERVICE_ACCOUNT: ${{ secrets.GDRIVE_SERVICE_ACCOUNT }}

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4 # 💡 升級到 v4

      - name: Set up Python
        uses: actions/setup-python@v5 # 💡 升級到 v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          # 💡 確保安裝所有必要的套件，避免 ModuleNotFoundError
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
          pip install resend google-api-python-client google-auth-httplib2 google-auth-oauthlib python-dotenv

      - name: Run Market Update
        run: |
          # 💡 使用 -u 參數強制讓 python 即時印出 log，方便在 GitHub Actions 監控進度
          python -u main.py ${{ matrix.market }}

      - name: Notify System Failure
        if: failure()
        run: |
          python -c "
          import os, requests
          token = os.getenv('TELEGRAM_BOT_TOKEN')
          chat_id = os.getenv('TELEGRAM_CHAT_ID')
          market = '${{ matrix.market }}'.upper()
          msg = f'❌ 【系統崩潰】市場: {market}\n原因：GitHub Actions 執行失敗，請檢查日誌。'
          if token and chat_id:
              requests.post(f'https://api.telegram.org/bot{token}/sendMessage', json={'chat_id': chat_id, 'text': msg})
          "
